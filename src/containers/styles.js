import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    zIndex: 1,
    position: 'relative',
  },
  app: {
    flex: 1,
    zIndex: 1,
  },
  statusBar: {
    zIndex: 11,
  },
});
export default styles;
